 group_visibility:
    sequence:
      - service: group.set_visibility
        data_template:
          entity_id: '{{ entity_id }}'
          visible: '{{ is_state(cond, visible_state) }}'
      # - service: persistent_notification.create
      #   data_template:
      #     title: "Script set_visibility called"
      #     message: >
      #       '{{ entity_id }}' , '{{states(cond)}}', '{{visible_state}}', '{{is_state(cond, visible_state)}}',
      #       {% if is_state(cond, visible_state) %} visible
      #       {% else %} not visible
      #       {% endif %}
